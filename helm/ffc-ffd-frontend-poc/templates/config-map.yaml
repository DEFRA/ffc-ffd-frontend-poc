{{- include "adp-helm-library.container-config-map" (list . "ffc-ffd-frontend-poc.config-map") -}}
{{- define "ffc-ffd-frontend-poc.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
